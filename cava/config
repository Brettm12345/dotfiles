[general]
bar_width = 2
framerate = 60
autosens = 1

[input]
method = fifo
source = /tmp/mpd.fifo

[output]
method = ncurses
channels = stereo
raw_target = /dev/stdout
data_format = binary
bit_format = 16bit
ascii_max_range = 1000

[color]
foreground = blue
gradient = 0

[smoothing]
integral = 90
monstercat = 1
waves = 0
gravity = 100

# vim:ft=config
